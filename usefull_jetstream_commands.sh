# authentication library
composer require laravel/jetstream

# session library
php8.3 artisan jetstream:install livewire

# after running this commands, you should see new pending migrations and a lot of modifications in all project

# running npm install to complete livewire
npm install

# start livewire
npm run dev